import { ThemedText } from '@/components/ThemedText';
import { SafeAreaView } from 'react-native-safe-area-context';
export default function MapsScreen() {
   return (
      <SafeAreaView>
         <ThemedText>Maps</ThemedText>
      </SafeAreaView>
   );
}
